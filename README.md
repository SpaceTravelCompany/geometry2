# geometry2

Odin `shared/geometry/linalg_ex` 의 1:1 Zig 0.16 포팅 (모듈명: `linalg`).

## 빌드

```bash
zig build check   # 컴파일 체크
zig build test    # 단위 테스트 실행
zig build install # 실행 파일 빌드
zig build run-test # sanity 테스트 실행
```
